## Task 13 report — Cursor rules + CLAUDE.md

- Added Cursor rules under `.cursor/rules/`: `bloc-architecture.mdc`, `code-style.mdc`, `dart-style.mdc`, `app-strings.mdc`, `shared-widgets.mdc`.
- Captured the repo’s intended `lib/` layout + architecture rules in root `CLAUDE.md` (feature-first, domain/data, DI via `RepositoryProvider`, Flame one-shot callbacks).
- Ported style rules from `unnati-mobile-new` and adapted them to this repo (paths/globs).

